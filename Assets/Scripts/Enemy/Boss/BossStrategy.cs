using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using BossAI;

public class BossStrategy : MonoBehaviour
{
    private AttackSelector attackSelector = new AttackSelector(null);
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
